# Experiment Cloud Component

Scope:
- create and run browser-based cognitive tasks and surveys
- capture participant responses and timing data
- support randomization and versioned experiment configs

Keep runtime and experiment definitions decoupled so protocol updates do not require code rewrites.